9:00 복습

오전 9:14 2022-11-09
<함수 복습>

감동 ㅠㅠ ❤️
매개변수 .. 
 소괄호 중괄호


기본적인 구조를 다 알게 

화살표 함수
화살표 함수가 1개인 경우 굉장히 유용하게 쓸 수 있을 듯 

2줄이상되면 중괄호 생략하면 절대 x
중괄호는 코드가 꼭 한줄만 있을 때 중괄호 생략 가능 

코드가 여러줄이면 
어디서 어디까지 if인지 for인지 개발자가 알려줘야해 
ㄴ 코드가 2줄이상이면반드시 묶어주어야 합니다. 

코드가 한 줄인 함수 

중괄호, return키워드  생략 가능



오전 9:19 2022-11-09
함수 정리 ,
중괄호, 소괄호 부분 설명해주셔서 좋았당.❤️

----------------------------------------------------------------------
오전 9:21 2022-11-09 

document : html을 관리하는 객체이다. 
●
DOM 객체를 제어하면 HTML 태그 제어 효과 발생

트리구조, DOM트리

한개한개의 객체는 5가지의 특징을 가지고 있는데,
ex.
🟨DOM 객체는 5개의 요소로 구성🟨
프로퍼티
메서드
컬렉션
이벤트 리스터
CSS3스타일



●

Document.getElementById(“firstP”).style.color= “red”;
P는 객체에요
Id 값으로 dom객체 찾기
Document.getElementById

●
DOM객체의 CSS3스타일 동적 변경
style 객체는 HTML 태그의 CSS3 스타일 시트 정보를 가짐 
style 객체를 이용하면 이미 출력된 HTML 태그의 모양 변경이 가능
DOM객체의 style 프로퍼티로 접근

CSS3 스타이리 프로퍼티는 다음과 같이 사용
backgorund-color ☞backgroundColor
font-size ☞  fontSize


★
변수명 - 카멜케이스 혹모양으로 만들기

this , 객체인 '나 자신을' 가르키는 것

div 태그 자신의 배경을 orange색으로 변경
<div onclick = "this.style.backgroundColor = 'orange'">
클릭이벤트 걸기



<body>
<script>
function change(obj, size, color) {
obj.style.color = color;
obj.style.fontSize = size;
}
</script>
<h3>this 활용</h3>
<hr>
<button onclick="change(this, '30px', 'red')">버튼</button>
<button onclick="change(this, '30px', 'blue')">버튼</button>
<div onclick="change(this, '25px', 'orange')">
여기를 클릭하면 크기와 색 변경
</div>
</body>

ㄴ 색깔로 표현하는 것 좋았다



p.34의 방법은 잘 사용하지는 않는다.

class 속성으로 DOM 객체 찾기



P.38
ID값,  

CLASS값

CSS선택자 - 쿼리   셀렉터 자리 다 넣을 수 있다  

문제점 : 항상 동일한 값이 있으면 첫번째 태그만 찾는다는게 문제점이다.

querySelectorAll 
모두 다 찾게 된다. 콤마로 여러개를 나열해서 찾을 수 있다는 것.
콤마 개수는 제한이 없다.

---
p.45
문서의 동적 구성
DOM 객체 동적 생성

var newDIV = document.createElement("div);
newDIV.innerHTML = "새로 생성된 DIV입니다."

---
DOM 트리에 삽입
//

DOM 객체 삭제
부모가 자식을 잘라내야한다.


태그를 동적으로 , 움직일수 있게 만들 수 있다.



오전 9:46 2022-11-09
문제풀이시간 

오전 9:55 2022-11-09 
쉬는시간 10시까지 

confirm("메시지")함수      -     ❤️메시지라고 표현한 내용이 너무 좋았음

var ret= confirm("전송할까요");
확인은 true
취소는 false

🟠🟠🟠🟠🟠
●
화살표 함수

let 함수이름 (arg1. arg2 , ... ,argn) =>{
// 프로그램 코드
// 결과를 리턴하는 return 문
}



<오버라이딩>
부모로부터 상속받은 클래스를 
자식이 재정의 하는 것 

<오버로딩 >
함수 이름이 똑같고 똑같은 함수를 똑같이 구현하는 것 
위에 sleep 있는데 또 똑같은 sleep을 만드는 것인데,
매개변수 개수가 다른 것 !!!!!

▶원래 백엔드 공부할 때 class 1주일동안 공부한다.



---
instanceof
🟠🟠🟠🟠🟠
틀 설계도 탬플릿 구조도 

붕어빵틀 : 클래스 - 붕어빵 : 인스턴스

생성자함수
사과 오렌지 출력하라

1)생성자 함수로 객체를 만드는 방법이 있고
2)클래스로 객체를 만드는 방법이 있다.

p.8  생성자 함수 VS 클래스
1) 생성자 함수
new 키워드가 생략되면 일반 함수로 호출됨
function 키워드로 정의

2) 클래스
new 키워드 생략시, 타입 에러 발생
class 키워드로 정의



class 소괄호를 넣지 않은 함수!

클래스는 자바스크립트에서 함수이긴 함수인데 ()가 없는 함수이다!

첫글자는 대문자로 하는 것이 개발자간의 관례이다.


let으로 바꾸기로 

-------------------------------------------------



🟠🟠🟠🟠🟠

값이 없는데 
다른 함수에게 접근 하고 수정할 수 있는 접근자 프로퍼티


get은 함수호출이되면 결과값을 반환해주는, 가져오는 함수값
set은 결과를 저장해주는 함수값






------------------------------------------------
프라이빗 변수는 외부에서 함부로 건들이지 못하게 하는 것 
, 직접적으로 접근하는 방법을 막아놓는것
그러면 어떻게 접근?   우회해서! 공부를 하면 우회해서 갈수 있어요 

함수를 통해서 내부의 프라이빗 값을 더해서 

값을 가지고 올때도 마찬가지. 

값을 우회해서 가져옴 

걔가 리턴을 해줌


값을 보고 싶을 때 get
값을 바꾸고 싶을 때 set 을 붙임

관리자만 이 방법을 알고 있어야 함

너무너무 중요한 값들은 숨겨놓고
전문가만 접근할 수 있게 해주는 것이 gette와 setter
-----------------------------------------------------

<상속>

class 상속이 가능

부모가 가진걸 그대로 물려받음과 
동시에 자신만이 하고 싶은 것을 할 수 있는 것 이 상속


상속받아 만들어진 클래스의 특징
자식입장에서, 물렵당느 기능에 필요한 기능을 추가하여 정의할 수 있다.

extends :상속을 한다는 



장점 : 코드 재사용, (공통점인 중복) 중복제거 ,





🟠🟠🟠🟠🟠

#1 객체에다가 데이터 저장 해야 key값으로 검색이 가능
x변수, 배열x
인풋태그  사용

#2 
클래스안에다가 저장할 수 있는가 


클래스만들어주시고 
각각의 고객 2명 만들고
 입금, 인출이 가능하게

#3랜덤한 숫자 보이게


학생의 정보를 저장하는 프로그램
이름 나이 기타등등 을 저장할 수 있는 프로그램
결과를 검색할 수 있는 프로그램
을 클래스로 이쁘게 만들어 주세요







오후 3:42 2022-11-09

🟠🟠🟠🟠🟠
이전기수 ,포트폴리오 보여달라고 요청하기 ok      
 ㄴ 다음부터  수첩,필기구  가져갈 것 





원페이지 표현방식  ppt 발표하는 느낌으로 













































